<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="AppFontStyle">
        <item name="android:textColor">@color/colorDarkGrey</item>
    </style>

    <style name="BannerFontStyle" parent="AppFontStyle">
        <item name="android:textSize">@dimen/text_size_banner</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">@color/colorLightGrey</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <style name="InfoFontStyle" parent="AppFontStyle">
        <item name="android:textSize">@dimen/text_size_info</item>
        <item name="android:textColor">@color/colorDarkGrey</item>
    </style>

    <style name="TitleFontStyle" parent="AppFontStyle">
        <item name="android:textSize">@dimen/text_size_title</item>
        <item name="android:textColor">@color/colorBlue</item>
    </style>
    <style name="HeaderFontStyle" parent="AppFontStyle">
        <item name="android:textSize">@dimen/text_size_header</item>
    </style>
    <style name="RegularFontStyle" parent="AppFontStyle">
        <item name="android:textSize">@dimen/text_size_regular</item>
    </style>
    <style name="DescriptionFontStyle" parent="AppFontStyle">
        <item name="android:textSize">@dimen/text_size_description</item>
    </style>
</resources>
